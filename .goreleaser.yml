version: 2

builds:
  - id: nuon-ext-ctx
    main: .
    binary: nuon-ext-ctx
    ldflags:
      - -s -w -X github.com/nuonco/nuon-ext-ctx/cmd.BuildVersion={{ .Version }}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - id: nuon-ext-ctx
    builds:
      - nuon-ext-ctx
    format: tar.gz
    name_template: "nuon-ext-ctx-{{ .Os }}-{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
